@inherits Custom.Hybrid.Razor12
@using ToSic.Razor.Blade;

@{
  var termGroups = AsList(App.Data["Term"])
    .GroupBy(l => l.Title.Substring(0, 1).ToUpper())    // group by first letter
    .OrderBy(s => s.Key);                               // sort asc
}

<div class="app-glossary4" @Edit.TagToolbar(toolbar: "contentitems?contentType=Term")>
  <nav class="navbar navbar-default">
    <div class="navbar-header" data-toggle="collapse" data-target="#glossaryNav" aria-expanded="false">
      <span>@Resources.GlossaryMenu</span>
      <span class="collapsed app-glossary4-toggle" title="@Resources.GlossaryMenu" ></span>
    </div>
    <div class="collapse navbar-collapse" id="glossaryNav">
      <ul class="nav navbar-nav app-glossary4-letterlist">
      @foreach(var grp in termGroups) {
        <li><a data-hash="@grp.Key">@grp.Key</a></li>
      }
      </ul>
    </div>
  </nav>
  <div class="app-glossary4-terms">
    @Html.Partial("./_Glossary.PartTermGroups.cshtml", new { TermGroups = termGroups } )
  </div>
</div>

@Html.Partial("./_Glossary.PartAssets.cshtml")